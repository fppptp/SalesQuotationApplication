@* @using SQTWeb.Models.QTMS
@model COM_Trs_SalesQuotation
@{
    ViewData["Title"] = "Edit Quotation";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-1">Edit quotation</h1>
        <p class="text-muted mb-0">@Model.QuoteNo</p>
    </div>
    <div class="d-flex gap-2">
        <a asp-action="Details" asp-route-id="@Model.QuoteId" class="btn btn-outline-primary">Details</a>
        <a asp-action="Index" class="btn btn-outline-secondary">Back</a>
    </div>
</div>

<form asp-action="Edit" method="post">
    <input asp-for="QuoteId" type="hidden" />
    <partial name="_QuotationForm" model="@Model" />
</form>
 *@