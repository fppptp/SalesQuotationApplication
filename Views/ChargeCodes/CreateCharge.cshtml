@* @using SQTWeb.ViewModels
@model ChargeCodeInputModel
@{
    ViewData["Title"] = L["Page_NewChargeCode"].Value;
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-1">@L["Page_NewChargeCode"]</h1>
        <p class="text-muted mb-0">Define a new standard charge code for use in quotations and rate cards</p>
    </div>
    <a asp-action="ChargesList" class="btn btn-outline-secondary">@L["Btn_Back"]</a>
</div>

<form asp-action="CreateCharge" method="post">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

    <div class="card mb-4">
        <div class="card-header bg-white">
            <h2 class="h5 mb-0">@L["Sec_Header"]</h2>
        </div>
        <div class="card-body">
            <partial name="_ChargeCodeForm" model="Model" />
        </div>
    </div>

    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">@L["Btn_Save"]</button>
        <a asp-action="ChargesList" class="btn btn-outline-secondary">@L["Btn_Cancel"]</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/chargeCode-editor.js" asp-append-version="true"></script>
}
 *@